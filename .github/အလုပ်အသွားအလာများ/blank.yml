# ဤသည်မှာ လုပ်ဆောင်ချက်များနှင့် စတင်ရန် ကူညီရန် အခြေခံအလုပ်အသွားအလာတစ်ခုဖြစ်သည်။
့လေ့လာ
အမည်-ဇော်မိုး၊
့
# အလုပ်အသွားအလာလည်ပတ်မည့်အချိန်ကို ထိန်းချုပ်သည်။
၀ယ်တယ်။ 07.oo
  #"ပင်မ"ဌာနခွဲအတွက်သာ တွန်းအားပေးခြင်း အစုလိုက်ဆွဲထုတ်ခြင်း တောင်းဆိုမှုဖြစ်ရပ်များတွင် အလုပ်အသွားအလာကို အစပျိုးထုတ် ၉၊
  တွန်းသည်။
    အကိုင်းအခက်    : [ "ပင်မ" 
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch :
့
# အလုပ်အသွားအလာ လည်ပတ်ခြင်းကို ဆက်တိုက် သို့မဟုတ် တဆက်တည်း လုပ်ဆောင်နိုင်သည့် တစ်ခု သို့မဟုတ် တစ်ခုထက်ပိုသော အလုပ်များဖြင့် ဖွဲ့စည်းထားသည်။
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
